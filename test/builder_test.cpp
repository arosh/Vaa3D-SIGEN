#include <gtest/gtest.h>
#include <builder/builder.h>
using namespace sigen;
TEST(builder, convert_to_neuron) {
  std::vector<std::shared_ptr<cluster>> data;
  const double scale_xy = 0.5;
  const double scale_z = 2.0;
}
